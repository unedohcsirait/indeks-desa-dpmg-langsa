## Packages
recharts | For visualizing dimension scores (Radar Chart)
framer-motion | For smooth transitions and animations
lucide-react | For beautiful icons (already in base, but emphasizing usage)
clsx | For conditional class merging
tailwind-merge | For merging tailwind classes

## Notes
Authentication is handled via Replit Auth (useAuth hook).
API endpoints are defined in @shared/routes.
Using Shadcn UI components for layout and form elements.
